RUNTIME=sandbox
ASSISTANT_NAME=Assistant
CLAUDE_CODE_OAUTH_TOKEN=__PASS:claudeclaw/anthropic-oauth-token__
TELEGRAM_BOT_TOKEN=__PASS:claudeclaw/telegram-bot-token__

# Fish Audio (TTS) — used by the built-in voice extension
FISH_AUDIO_API_KEY=__PASS:api/fish-audio-main__

# OpenAI — used by vision captioner for DeepSeek runner (and future tools)
OPENAI_API_KEY=__PASS:lightrag/openai-api-key__

# Exchange On-Premise (optional). Leave blank to disable the exchange MCP tool.
EXCHANGE_SERVER=mail.example.com
EXCHANGE_USERNAME=DOMAIN\\user
EXCHANGE_EMAIL=user@example.com
EXCHANGE_PASSWORD=__PASS:exchange/password__

# Bitrix24 (optional)
# Preferred: OAuth via published app (auto-refreshes, survives token rotation).
# All five OAuth keys must be set together. Channel falls back to BITRIX24_WEBHOOK if any are missing.
BITRIX24_CLIENT_ENDPOINT=__PASS:bitrix24/oauth-client-endpoint__
BITRIX24_ACCESS_TOKEN=__PASS:bitrix24/oauth-access-token__
BITRIX24_REFRESH_TOKEN=__PASS:bitrix24/oauth-refresh-token__
BITRIX24_CLIENT_ID=__PASS:bitrix24/yupiter-client-id__
BITRIX24_CLIENT_SECRET=__PASS:bitrix24/yupiter-client-secret__
# Legacy webhook auth (deprecated — kept for backward compatibility, ignored if OAuth is configured).
BITRIX24_WEBHOOK=__PASS:bitrix24/webhook-token__
BITRIX24_ALLOWED_USER_ID=

# Home Assistant MCP server (optional). Both must be set together to enable mcp__homeassistant__* tools.
HOMEASSISTANT_BASE_URL=http://185.108.209.213:8123
HOMEASSISTANT_LLAT=__PASS:homeassistant/zabava-mcp-llat__

# Instagram extension config lives in the instance that owns the IG account.
# This main instance can load the extension code but stays idle when
# INSTAGRAM_* env vars are absent.
